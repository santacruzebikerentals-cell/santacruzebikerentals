[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

  [build.environment]
SECRETS_SCAN_OMIT_KEYS = "VITE_SQUARE_ACCESS_TOKEN"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
